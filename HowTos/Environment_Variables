Navigate to /home  #terminal command cd -> enter

nano .bash_profile # hidden file in the home directory

type: export ALL_CAPS_VAR_NAME="SENSITIVE_DATA" # Make sure there is no space before or after the equals sign

When importing into your python script type:
import os
VAR = os.environ.get('ENVIRONMENT_VARIABLE_NAME')

