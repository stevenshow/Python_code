# Creates a dictionary with a default value if none exist, else appends item to dict (or whatever other operator you are using on the data structure)
dict.setdefault(key, []).append(value) 
    link to stack overflow = t.ly/YH5B
    link to docs = t.ly/MN64

# How to sort a dictionary based on the values
    dict1 = {1: 1, 2: 9, 3: 4}
    sorted_dict = {}
    sorted_keys = sorted(dict1, key=dict1.get)  # [1, 3, 2]

    for w in sorted_keys:
        sorted_dict[w] = dict1[w]

    print(sorted_dict) # {1: 1, 3: 4, 2: 9}