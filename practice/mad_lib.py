#Mad Lib Generator
#[] Get user input (noune, verb, name, adjective, etc)
#[] Print story using user's input
#use replace() function to replace blanks with user's words
#use a dictionary to fill up with users words depending on if they are nouns or verbs etc


def get_user():
    story_length = input('How long would you like the story to be? (S)hort, (M)edium, or (L)ong: ')
    if (story_length.lower() == 's'):
        pass
    elif (story_length.lower() == 'm'):
        pass
    elif(story_length.lower() == 'l'):
        pass

